# Zxtract Figma 原型状态

文件：https://www.figma.com/design/j9B1DnbnZx1cABS5adZZg7

状态：可编辑初版。Figma Starter MCP 调用额度已耗尽，最后一次画布修正未写入；这些修正已在 WPF 实现阶段直接吸收。2026-09-24 已按此原型重构主窗口，原型文件仍保留作为设计依据。

## 已创建

- 单页 `0:1`：01 · 交互原型。
- 使用说明 `2:195`，已设原型起点。
- 普通解压：空列表 `2:196`、待开始 `2:197`、处理中 `3:2`、已完成 `3:3`。
- 文件夹解压：待扫描 `2:198`、处理中 `2:199`、已暂停 `2:200`、需处理 `2:201`、密码重试完成 `2:202`。
- 1024×640 紧凑窗口 `2:203`。
- 共用密码 `2:204`、修改密码并重试 `2:205`、分卷任务详情 `2:206`、更多选项 `2:207`。
- 使用 Simple Design System 按钮、页签和输入组件实例；中文本地化为 Noto Sans SC。现有应用的 Microsoft YaHei UI 在 Figma 当前环境不可用。
- 固定操作区和独立列表滚动区；主要入口、暂停/继续、密码弹层、单项重试已连线。

## 已完成视觉检查

已查看处理中、小窗口和共用密码画板截图。主界面层级、中文排版和列表对齐基本正常；发现以下需要修正的问题。

## 已在代码实现中处理

1. 弹层底部溢出：共用密码从 510 高调整到至少 560；分卷详情从 490 到至少 560；更多选项从 410 到至少 500。调整后复核。
2. 输入实例外层高 36，但内部 `Input` 为 46，造成布局溢出。将内部上下 padding 从 12 调至 7、高度改为 36，或者统一为 46 并增加父容器空间。
3. 失败画板当前选中“需处理”，却显示所有六行；需仅保留课程资料、图标合集、项目备份三行，或将选中项改为“全部”。
4. 小窗口只有四个示例行；应补齐另外两个已完成行并验证独立列表滚动，顶部导航保持不动。
5. 修正尚未精细化的示例连线：所有“详情 / 打开位置 / 查看日志”目前共用分卷详情弹层，应分别实现或移除不准确的连线；筛选按钮不应跨模式跳转；切换模式应保留各自示例任务状态。
6. “仅扫描”当前跳转到处理中，应补充扫描完成状态，不执行解压；运行期间不应允许重复扫描。
7. 普通与文件夹处理中画板尚未自动推进至完成/需处理状态，可添加明确的演示推进按钮或延时跳转；注意暂停时不要触发推进。
8. 密码弹层的候选编辑、显示、库管理、规则管理，以及选项选择，目前主要是视觉展示，不是完整交互。至少补足快速复用与应用后摘要更新的演示。
9. 重新核验全部文字字体、边界溢出、跳转目标和弹层关闭行为；修正后截图，再交付最终版本。

实现对应文件：`src/ExtractUtil.App/MainWindow.xaml`、`src/ExtractUtil.App/ViewModels/MainViewModel.cs`、`src/ExtractUtil.App/ViewModels/JobItemViewModel.cs`。应用实现不依赖 Figma 原型中的模拟进度或演示数据。

所有路径、密码掩码和进度均为虚构演示数据；原型不执行文件操作。

## OpenPencil 镜像

2026-09-24 已将原型结构与 WPF 最终修正同步到本地 OpenPencil 文档：

- 文件：`C:\Users\excel\Documents\Zxtract.fig`
- 页面：`Zxtract · UI 原型`
- 内容：1 个使用说明画板、8 个主界面状态画板、4 个面板/详情画板，共13 个顶层画板
- 重建脚本：`tools/Sync-ZxtractOpenPencil.ps1`
- 校验：683 个节点已完成边界检查，严重溢出为 0；整页预览见 `docs/previews/zxtract-openpencil-main.png`

OpenPencil 0.15.1 Windows 版存在桌面端鉴权握手问题；当服务仅监听 `127.0.0.1`
时，需以空的 `OPENPENCIL_MCP_AUTH_TOKEN` 启动 HTTP MCP 服务，否则桌面端可能持续
显示 `no_app`。
